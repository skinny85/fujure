apply plugin: 'java'

repositories {
    mavenCentral()
}

task run(type: JavaExec) {
    main = "Main"
    classpath = sourceSets.main.runtimeClasspath
    args = ["example.fjr"]
}

dependencies {
    compile project(':src:main:fjc-bnfc')
}
